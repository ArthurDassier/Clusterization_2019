apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: result
  annotations:
    ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - http:
      paths:
        - path: /
          backend:
            serviceName: result
            servicePort: 5001