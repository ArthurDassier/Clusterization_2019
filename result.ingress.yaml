apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: result
  annotations:
    kubernetes.io/ingress.class: traefik
spec:
  rules:
  - http:
      paths:
        - path: /result
          backend:
            serviceName: result
            servicePort: 80